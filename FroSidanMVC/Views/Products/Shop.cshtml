@model ShopVM[]
@{ViewBag.Title = "Shop";}

<br />
<br />
<div class="container">
    <div class="row row-cols-4">
        @foreach (var item in Model)
        {
            <div class="col-md-3">
                        <a asp-controller="Products" asp-action="Detail" asp-route-id=@item.Id> @*Länkning funkar ej, glöm inte fixa*@
                <div class="card text-center">
                    <img src="~/Images/@item.ThumbnailUrl" width="100%;" />
                    <div class="card-body">
                        <h5 class="card-title">@item.Name </h5>
                            <p class="card-text">@item.SubCategory</p>
                        <a href="#" onclick="AddToCart(@item.Id)" class="btn btn-success">Lägg till i varukorg 🛒</a>
                        <a href="#" onclick="RemoveSingle(@item.Id)" class="btn btn-success">Ta bort (1)</a>
                    </div>
                </div>
                        </a>
            </div>
        }
        <div class="col-md-3">
            <div class="card text-center">
                <div class="card-body">
                    <a href="#" onclick="ClearCart()" class="btn btn-success">Töm Varukorg</a>
                </div>
            </div>
        </div>
    </div>
</div>

<br />

    @*<br />

        <div class="row">
            <div class="col-sm-6">
                <div class="card text-center">
                    <img src="~/Images/chilipeppar.png" width="100%;" />
                    <div class="card-body">
                        <h5 class="card-title">Chili</h5>
                        <p class="card-text">Het chilipeppar</p>
                        <a href="#" class="btn btn-success">Lägg till i varukorg 🛒</a>
                    </div>
                </div>
            </div>
            <div class="col-sm-6">
                <div class="card text-center">
                    <img src="~/Images/chilipeppar.png" width="100%;" />
                    <div class="card-body">
                        <h5 class="card-title">Chili</h5>
                        <p class="card-text">Het chilipeppar</p>
                        <a href="#" type="submit" class="btn btn-success">Lägg till i varukorg 🛒</a>
                    </div>
                </div>
            </div>
            <div class="col-sm-6">
                <div class="card text-center">
                    <img src="~/Images/chilipeppar.png" width="100%;" />
                    <div class="card-body">
                        <h5 class="card-title">Chili</h5>
                        <p class="card-text">Het chilipeppar</p>
                        <a href="#" class="btn btn-success">Lägg till i varukorg 🛒</a>
                    </div>
                </div>
            </div>
        </div>*@


    @section scripts
{
        <script type="text/javascript">
            function AddToCart(id) {
                $.ajax({
                    url: "/AddToCart/" + id,
                    type: "GET",
                    success: (result) => console.log(result)/*$("#result" + id).html(result)*/
                });
            };

            function ClearCart() {
                $.ajax({
                    url: "/ClearCart/",
                    type: "GET",
                    success: (result) => console.log(result)
                });
            };

            function RemoveSingle(id) {
                $.ajax({
                    url: "/RemoveSingleFromCart/" + id,
                    type: "GET",
                    success: (result) => console.log(result)
                });
            };
        </script>
    }
